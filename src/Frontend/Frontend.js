// send request to server for a game's state
// http://localhost:3000/games sends a game's representation as a json
// arr = parse(data)

//board representation : an array of size 32 , in each entry there is a tuple of size 4
// [(piece_type, player_color, position, is_in_game) ..... (....)]